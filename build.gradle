apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'Repl.App'

jar {
    baseName = 'WebREPL'
    version = '0.1.0'
    manifest {
        attributes(
            'Main-Class': 'Repl.App'
        )
    }
}
